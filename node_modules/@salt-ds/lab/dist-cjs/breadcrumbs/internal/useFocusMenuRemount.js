'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var core = require('@salt-ds/core');
var react = require('react');

function useFocusMenuRemount(key) {
  const ref = react.useRef(null);
  const shouldFocusOnMount = react.useRef(false);
  const previousKey = core.usePrevious(key);
  react.useEffect(() => {
    if (ref.current && shouldFocusOnMount.current && previousKey !== key) {
      ref.current.focus();
      shouldFocusOnMount.current = false;
    } else {
      shouldFocusOnMount.current = false;
    }
  }, [previousKey, key]);
  return { ref, shouldFocusOnMount };
}

exports.useFocusMenuRemount = useFocusMenuRemount;
//# sourceMappingURL=useFocusMenuRemount.js.map
