function getAnnouncement(itemCount, firstItem) {
  return itemCount === 0 || itemCount === void 0 ? "no results" : `${itemCount} ${itemCount > 1 ? "results" : "result"}${firstItem ? `, ${firstItem}` : ""}`;
}

export { getAnnouncement };
//# sourceMappingURL=getAnnouncement.js.map
