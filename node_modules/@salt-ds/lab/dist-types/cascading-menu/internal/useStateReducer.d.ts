import { type menuAction } from "./CascadingMenuAction";
import type { flattenedMenuState } from "./stateUtils";
export declare type stateItem = {
    id: string;
    highlightedItemIndex: number | null;
};
declare type state = stateItem[];
export declare function useStateReducer(menusDataById: flattenedMenuState, isNavigatingWithKeyboard: boolean): (state: state, action: menuAction) => state;
export {};
