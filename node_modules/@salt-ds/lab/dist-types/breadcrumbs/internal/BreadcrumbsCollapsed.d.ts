import { type ReactNode } from "react";
import type { CascadingMenuProps } from "../../cascading-menu";
import { type MenuButtonProps } from "../../menu-button";
export interface BreadcrumbsCollapsedProps extends Omit<MenuButtonProps, "CascadingMenuProps"> {
    CascadingMenuProps?: CascadingMenuProps;
    accessibleText?: string;
    children: ReactNode;
    className?: string;
}
export declare const BreadcrumbsCollapsed: ({ children, CascadingMenuProps, accessibleText, ...rest }: BreadcrumbsCollapsedProps) => import("react/jsx-runtime").JSX.Element;
